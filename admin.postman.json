{
  "info": {
    "name": "admin",
    "schema": "https://schema.postman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "POST /approve-user/:userId",
      "request": {
        "method": "POST",
        "url": {
          "raw": "{{base_url}}/approve-user/:userId",
          "host": ["{{base_url}}"],
          "path": ["approve-user", ":userId"]
        },
        "headers": []
      }
    }
  ]
}
